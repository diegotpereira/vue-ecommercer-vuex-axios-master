export function setProdutos(estado, val) {
    estado.produtos = val
}

export function setProduto(estado, val) {
    estado.produto = val
}

export function setCarregar(estado, val) {
    estado.uploadingData = val
}

export function setCarrinho(estado, val) {
    estado.carrinho = val
}